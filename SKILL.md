---
name: ecommerce-ops-diagnosis
description: 电商运营全链路诊断总编排 Skill。Use when the user asks for 电商运营诊断、店铺诊断、产品体检、整链体检、主图/详情页/人群/数据/运营策略诊断，or wants to know whether a product or store works and what to do next. Routes through 主图诊断、详情页诊断、人群验证、数据分析、运营策略 with data-grounded, source-labeled conclusions.
---

# 电商运营诊断（总编排）

> 版本：v1.8.0｜作者：张发财｜公众号「财哥的电商思维」｜小红书「张发财的资料库」。免费个人使用，禁商业售卖，转载/分享请保留作者标识。

> 一台把「四诊断 ⟶ 运营策略」串成链的总台。你给一个产品 + 物料 + 真实数据，它按统一纪律带你走完整条诊断链，输出**可溯源、缺数据降级、不编不预测**的诊断与动作。
>
> 世界观：**真实数据至上**。模型擅长"这条障碍对这群人成不成立 / 这屏结构对不对 / 这个渠道是不是黑洞"（是非·分级·算术题），不擅长"百分之多少的人会怎样 / 改完涨多少"（频率·预测题）。本 Skill 只产前者，焊死不产后者。
>
> 📌 **出身**：本方法论经真实电商店铺整链实战检验、反复打磨——不是纸面框架。别的工具敢拍胸脯给你涨幅，本诊断只给**站得住的判断**。

---

## 🛠️ 在哪用 · 最快上手

- **Claude Code / Codex / Claw / 扣子**：装成 skill 调用；显式调用名用 `$ecommerce-ops-diagnosis`，中文说「电商运营诊断 / 店铺诊断 / 产品体检」也应触发。
- **豆包 / Kimi / ChatGPT 等普通 AI**：不装 skill 也能用——先用 `references/真实数据采集.md` 里的「📋 可直接粘贴的提示词」取数；其他段按本文路由，把对应 reference 或兄弟子 skill 的要求连同物料一起提供。
- **第一次没真实数据？** 别空跑：先按 `references/真实数据采集.md`（第⓪段）把真数据取到手再进诊断。模型不替你编。

---

## 🎯 这个 Skill 能做什么

| 你给我什么 | 我带你走哪段 |
|----------|------------|
| 一堆评论 / 后台报表 | **第⓪段·真实数据采集**：洗成客群占比/痛点/三联表，喂给后面各段 |
| 一张主图 + 情境 | **主图诊断**：这张图在这个情境下行不行、改哪 |
| 整套详情页截图 + 情境 | **详情页诊断**：五维审一遍、哪屏拖后腿 |
| 物料 + 真实客群构成 | **人群验证**：谁会卡、卡在哪、哪条障碍最致命 |
| 流量/客群/SKU 三张表 | **数据分析**：黑洞渠道 / 死流量人群 / 王者SKU 在哪 |
| 上面几段的诊断结论 | **运营策略**：按优先级出动作，每条标来源 |
| 一个店从头到尾 | **整链体检**：五段依次跑，前段喂后段 |

可整链走，也可单跑某一段（数据契约满足即可）。

---

## 🚦 触发与症状分诊

用户说「跑电商运营诊断」「给我这产品/店铺做个体检」「我这主图/详情页行不行」「谁会买」「这堆数据怎么读」「接下来该打什么」——进本 Skill。

**进门先分诊**：真实用户开口说的多半是**症状**，不是物料。先按下表定问题域，再进路由表对应段；用户直接给了物料（主图/截图/三张表）就跳过分诊、直接走路由表。

| 用户开口说 | 问题域 | 分诊到 | 为什么 |
|---|---|---|---|
| 「店铺掉了」「GMV 降了」「最近不行了」 | 销售下滑 | ④数据分析 | 先用原子拆解树定位掉在哪个变量，不猜 |
| 「有访客但没人买」「转化差」 | 转化 | ④定位约束 → ②/③ | 先判约束在详情页还是人群，再进对应段 |
| 「推广烧钱没产出」「ROI 撑不住」 | 投放效率 | ⚠️ 专项待建（暂走 ④+⑤） | 投放诊断是当前空缺域，先用原子树 + 策略段兜住，结论标"投放专项待建" |
| 「我这主图/详情页行不行」 | 物料诊断 | ①/② | 物料路由，见路由表 |
| 「谁会买」「谁会卡」 | 人群 | ③ | 物料路由，见路由表 |
| 「这堆数据怎么读」 | 数据 | ⓪/④ | 有三张表直接读④；只有评论/报表先走⓪ |
| 「我也说不清哪里有问题」 | 问题模糊 | 🧠术语审计（认知OS §0）先拆问题 | 问题没说清，任何诊断都是在糊弄 |

**分诊纪律**：分诊不明时只问**一个**澄清问题，不连环追问；定了域就说一句「这是 X 域的问题，走第 N 段」，然后开工。

---

## 🟥 贯穿红线（五段全程遵守，违任一返工）

1. **缺数据降级标注**——数据契约缺一不硬给结论，按降级路径走、并红标说明。
2. **不编百分比/频率**——「X% 的人会划走 / 命中率 30%」一律禁。频率题模型有效性≈0。
3. **不预测业绩**——CTR / 转化率 / 涨幅 / 销量 / GMV 任何数字预言一律禁；只给方向。
4. **来源必标**——每条结论挂 `[数据]` / `[推断]` / `basis`（哪几条情境/哪份诊断在起作用），可溯源。
5. **不自抓链接**——天猫/淘宝反爬抓不到；要诊断 → 发截图/文字。模型不替你抓、不替你编人口构成。
6. **不透支长期信任（儒家底线）**——短期 ROI 动作若伤害用户长期信任（虚假宣传、夸大功效、刷单刷评），一律标 `[信任透支]` 并禁止进入"推荐策略"。店铺的根基不是单次 GMV，是复购率和口碑复利。

> 产出前自己过一遍这六条，命中即改。

---

## 🧠 认知操作系统（速查路由 · 正文见 `references/认知OS.md`）

> 五段诊断是「做什么」，七维＋三向补充是「怎么思考」。完整方法论已拆到 `references/认知OS.md`——**走到哪段，先读对应章节再产出**；本表只作路由，不替代正文。

| 段 | 激活范式（认知OS 章节） | 必出标注 |
|----|----------------------|---------|
| 全段前置 | 维特根斯坦·术语审计（§0） | 开头一行 `术语对齐：…`，未对齐标 `[待澄清]` |
| ②详情页 | 系统论·约束定位（§3） | `系统约束：{环节} 证据：{数据}` |
| ③人群验证 | 奥地利学派·需求还原（§1）＋ 存在主义·焦虑面（§1补充） | `需求线：` 与 `焦虑面：` 成对出现 |
| ④数据分析 | 第一性原理·指标原子化（§2）＋ 系统论（§3） | 原子拆解树 + `根因：{原子变量}` |
| ⑤运营策略 | 博弈论·竞品预判（§4）＋ 斯多葛·控制圈（§6）＋ 儒家红线⑥ | `竞品回应：` + 控制圈标注 + `[信任透支]` 筛查 |
| 🔁自进化 / 🔄复诊 | 贝叶斯·先验→后验（§5）＋ 辩证法·矛盾转化（§3补充） | 引用标"据经验库§X" + 复诊加 `矛盾动态：` |


---

## 📋 数据契约（开工门槛）

| 段 | 必填 | 缺了怎么办 |
|----|------|-----------|
| 通用（每段都要） | 品类 / 客单价 / 人群 / 参数 / 竞品（+渠道） | 对应维度判断标 `[推断]`、basis 留空并注明"情境缺失"，不硬给 |
| 人群验证 额外 | 真实客群构成（人群占比 + 来源） | **降级**：只列障碍、全程"排序不可信"红标，不排序 |
| 数据分析 | 流量结构表 / 客群错位表 / SKU 矩阵表（手填真实数） | 哪张表空，该维度不算，不编数字 |

**数据完备度评分**：每段输出末尾附「数据完备度 x/10」。满分 10，每缺一个通用维度 −1.5，无视觉能力 −2。

---

## 🔗 链路与 handoff

```
⓪真实数据采集(评论/后台 → 客群占比·痛点·三联表)
        │ 喂真实输入(红线⑤要的真实客群构成就来自这)
        ▼
主图诊断 ─→ 详情页诊断 ─→ 人群验证 ──最致命障碍──▶ 详情页"怎么改"(手术)
                            ▲
            数据分析 ──客群错位──┘ (指导该代入哪些有份量的人群)
                │
            三份摘要(详情页 + 人群 + 数据) ──▶ 运营策略(按优先级出动作)
```

- **整链走**：主图→详情页→人群→数据→策略，前段产物喂后段。
- 关键接力：
  - 人群验证产「最致命障碍」→ 喂详情页"怎么改"，省掉"拍人群"翻车。
  - 数据分析识别「客群错位」（访问端 vs 支付端不一致）→ 指导人群验证该代入谁。
  - 运营策略自动消费 ①②③ 三份最新诊断摘要；缺哪份标"未做"、不基于它下结论。

---

## 🧭 路由表（每段：何时进 · 方法论在包内哪个文件）

| 段 | 何时进 | 方法论正文（包内相对路径） | 一句话纪律 |
|----|-------|--------------------------|-----------|
| ⓪ 真实数据采集 | 手里只有评论/报表、没现成真实数据 | `references/真实数据采集.md` | 真实计数可用；样本不代表盘子要标；不替你编 |
| ① 主图诊断 | 只看单张主图好不好 | 调子 skill **「主图情境诊断」**（`子技能/主图情境诊断/SKILL.md`） | 三层判断；好坏标 basis；缺情境→[推断]；禁 CTR 预测 |
| ② 详情页诊断 | 审整套详情页 / 详情页怎么改 | 调子 skill **「详情页架构师」**（`子技能/详情页架构师/SKILL.md`） | 🧠系统论·约束定位 — 审五维(结构/排序/视觉/因果/转化)；basis 取6维；禁涨幅预测 |
| ③ 人群验证 | 想知道"谁会卡、哪条障碍最致命" | 调子 skill **「人群验证引擎」**（`子技能/人群验证引擎/SKILL.md`） | 🧠奥地利学派·需求还原 — 8红线；严重度四档；每障碍带"❗判断不了的"+需求线；禁频率%；**必实际读卡片代入** |
| ④ 数据分析 | 有流量/客群/SKU 真实数据要读 | `references/数据分析.md` | 🧠第一性原理·指标原子化 — 三联表确定性算 flag；逐项拆到原子变量；AI 只加定性、不碰数字、不预测业绩 |
| ⑤ 运营策略 | 已有上游诊断，要落成动作 | `references/运营策略.md` | 🧠博弈论·竞品预判 + 斯多葛·控制圈 — 每条标来源(basis)；加竞品回应预判；区分可控/不可控；禁业绩预测 |

走某一段时，**⓪④⑤ 先读本 skill 的 references；①②③ 先读上表对应的包内子技能**，再严格按它的纪律执行。整链走就按 ⓪→⑤ 顺序，每段产一个简短摘要喂下一段。

> **横切查表**：主图/详情页/人群三段判"渠道适配·该主打什么·文案合不合规"时，查 `references/渠道与人群速查.md`（平台气质矩阵 + 购买决策驱动 + 平台合规禁用词），结论挂 basis。

---

## 🧭 任务后导航（每段收尾必出）

单跑一段不等于诊断结束。每段收尾，根据**本段结论信号**推荐 2-3 个下一步（每条带为什么）；若用户授权写存档，再同步写进本次存档的「下一步」字段。用户说了想做什么 → 按用户的来；下表是默认推荐，不是固定流程。

| 刚跑完 | 结论信号 | 推荐下一步 | 为什么 |
|---|---|---|---|
| ⓪采集 | 三联表/客群占比到手 | ④ 或 ③ | 真实数据齐了，进定量段 |
| ①主图 | 问题不止主图（卖点/人群层面） | ② / ③ | 主图救不了选错的卖点 |
| ②详情页 | 手术方案已出 | 改完带新截图回来复诊 | 复诊只判 delta |
| ③人群 | 最致命障碍在详情页结构 | ②手术 | handoff：障碍直接喂"怎么改" |
| ③人群 | 「判断不了的」清单过长 | ⓪补数据 | 不是诊断问题，是输入问题 |
| ③人群 | 缺真实客群构成、排序不可信 | ⓪ | 先拿到人群占比，排序才算数 |
| ④数据 | 客群错位（访问端≠支付端） | ③换代入对象 | handoff：错位指导代入谁 |
| ④数据 | 根因指到点击率/主图吸引力 | ① | 原子树指到哪就去哪 |
| ④数据 | 黑洞渠道/王者SKU 已定位 | ⑤ | 定位完了该出动作 |
| ⑤策略 | 动作清单已出 | 用户授权时写存档 + 挂 `/schedule` 催回填 | 出口闭环：等真实结果回来给假说升降级 |

---

## 🗂️ 诊断存档（单店状态 · 复诊与回填触发器的落点）

> **两库分工（别混）**：`data/经验库.md` 存**方法论资产**——跨店可复用的判例，走置信阶梯；`data/诊断存档/` 存**单店状态资产**——这家店此刻卡在哪，跟着该店的诊断-复诊循环走。一次性的本店事实进存档，不进经验库。

- **位置**：`data/诊断存档/{店铺}/{YYYY-MM-DD}-{段或主题}.md`，一次诊断一份快照，**不覆盖旧档**（辩证法的矛盾转化追踪要对比历史）。格式见 `data/诊断存档/_模板.md`。
- **何时写**：仅在本地自用库运行、或用户明确要求「写入存档 / 回填经验库 / 持久化本次诊断」时写；普通诊断默认只在回复中给出可复制的存档摘要，不擅自改库。
- **何时读**：复诊第 1 步读该店最新一份；首诊某店前也先扫一眼有没有旧档，别把复诊当首诊跑。
- frontmatter 的「未回填假说」字段存经验库 §0 的指针，是回填触发器的钩子（见 🔁 启动动作）。

---

## 🔁 自进化 · 判例制回路（🧠贝叶斯·先验→后验）

> 哲学：**让现实当唯一的老师，模型永远不许给自己打分。** 这台 skill 变准，不是模型"学会"了什么，而是**现实的判例攒多了、被归档了**。经验库 ＝ 判例卷宗，不是心得笔记。详见 `data/经验库.md`。
>
> 这也是把红线焊死的东西今天禁止的答案（"某人群某款转化高"）合法赚回来的唯一路径：它不是模型猜的，是攒够真实成交后从 `[推断]` 升 `[数据]`。
>
> 💡 **贝叶斯视角**：经验库 = 先验概率，每次新诊断 = 似然函数，真实成交回填 = 证据。P(H|E) ∝ P(E|H) × P(H)。详见 `references/认知OS.md` §5。

**四个动作，自用闭环或用户授权持久化时才完整执行；普通诊断默认只读先验、不写库：**

1. **启动·读先验 + 催回填**——开诊断前先 Read `data/经验库.md` 与本次相关的段/品类章节，把判例当先验，引用标"据经验库 §X（置信态/证据）"。再读该店最新存档的「未回填假说」指针：有未回填假说（§0 主表，沉睡区不催）→ 开场先问一句「上次留了假说 X，真实数字回来了吗？」——回来了走动作 3，没回来照常诊断、指针原样带进新存档。
2. **收尾·回写假说（需授权）**——用户授权持久化时，把本次新产生的是非假说写进经验库 §0 待验证隔离区（标来源/日期/证伪条件——证伪条件直接取自本次输出的「反证」字段）；未授权时只在回复末尾列出「可回写假说」。
3. **回填·棘轮升降级**——用户带真实结果回来时才动：假说被真实数据证实 → 升「单验/规律」；被推翻 → 退役到 §4。**升级入口只有这一个，模型自评不算。**
4. **收尾·写存档（需授权）**——用户授权持久化时，按 `data/诊断存档/_模板.md` 落一份本次快照（含红 flag、完备度、矛盾格局、未回填假说指针、下一步推荐）；未授权时只输出「存档摘要」。

**回路红线（违任一不写库）：**
- **意义闸（写库第一道·命题7）**：写任何条目前先过三问——①是不是是非命题（频率/占比＝伪命题，直接沉默、连 §0 都不进）？②是不是冒充知识的预测（只能当"待裁决问题"记 §0，永不作结论）？③说得出证伪条件吗（说不出＝废话，拒收）？不过闸连假说都不收。
- 只有**真实结果**能升置信；模型印象只能待在 §0，禁止当 basis。
- **频率/预测永不入库**——能沉淀的只有是非命题（障碍成立与否、渠道黑洞、王者 SKU），不许沉淀百分比或涨幅。
- **引用纪律**：只有「单验」及以上能进 basis；§0 的假说绝不当结论。
- **时钟是"真实数据到达"，不是时间**：没有新真实结果就别重跑评估（重跑只会把同一批结论再嚼一遍）。要催回填→挂 `/schedule` 提醒，loop 只催人喂真数据、不替人造数据。

---

## 🔄 复诊 · 收敛循环（🧠斯多葛·控制圈 + 贝叶斯·证据驱动）

> 整链不是一次性流水线，是会**收敛**的环。诊断 → 你去改 → 带着"改了什么"回来 → **只复诊 delta** → 判最致命障碍消解没 → 没消解再来一轮。
>
> 和自进化（🔁，跨会话）是两层、别混：**复诊判"结构问题改没改"**（是非题，模型能判）；**自进化判"改了之后真实数字动没动"**（回填，只有现实能判）。复诊的结论是后者的假说来源。

**何时进复诊**（必须有真实 delta，否则不跑——没有新改动/新数据的"复诊"＝原地打转、镜厅空转）：
- 用户说"按你说的改了，再看看"，或给了改后的新物料 / 新真实数据。

**复诊四步：**
1. **取上次结论**：读 `data/诊断存档/{店铺}/` 最新快照（最致命障碍 / 红 flag / 完备度 / 矛盾格局）。没存档就退化成首诊。
2. **认 delta**：这次改了什么、多了哪些真实数据。**只诊断变化面，不整链重跑。**
3. **判消解**（逐条对上次的红 flag）：
   - ✅ **消解**：结构问题真补上了（是非题，模型判）。
   - 🔁 **搬家**：旧障碍没了、改动又引出新障碍 → 记下，继续。
   - ❌ **仍在**：没改到点上 → 说清差在哪。
4. **判收敛 or 再迭代。**

**退出条件（收敛即停，别空转）：**
- 最致命障碍**消解** 且 无新红 flag → **收敛，停**。
- 完备度不再提升、障碍只在原地搬家 → **停**，说明"已到模型能判的边界，再循环无新信息"（命题5.6：到边界了别往不可说那边推）。
- **最多 3 轮**护栏：到顶仍未收敛 → 停、列"还卡在哪 + 需要什么真实数据/动作才能破"，交回用户，不自我空转。

> 💡 **斯多葛视角**：复诊收敛后的收尾，强制标注本轮行动的控制圈——哪些障碍是你改了之后解决的（可控），哪些受制于外部因素未解决（不可控→设观察哨）。不把不可控的失败当自己的失败。

**复诊收尾**：自用闭环或用户授权持久化时，照常走自进化收尾——把"某障碍改 X 后是否消解"作为新假说写进经验库 §0（过意义闸、标证伪条件），等真实数字回填时再升降级；未授权时只输出可回写假说。

---

## 📦 输出约定

- **前置**：诊断开始附 `术语对齐：转化率=|...|UV价值=|...|`（🧠维特根斯坦）。
- 每段结论分两类标清楚：`[数据]`＝有真实数据支撑；`[推断]`＝情境/逻辑推导，需真实数据回填。
- **单一判断纪律**：数据契约满足的维度，必须给单一明确判断，禁止「可能是A也可能是B」的对冲话术；契约不满足才允许降级（红线①）。骑墙和硬编都算违规。
- **反证检查**：每条核心判断挂一行 `反证：若{可观察事实X}出现，本判断翻`（意义闸第三问前移到每条判断；收尾回写 §0 时证伪条件直接从这抄）。
- 整链走时，每段产一个简短摘要（结论 + cantJudge + 完备度），作下一段输入。
- ④数据分析：附原子变量拆解树，标 `根因：|{原子变量}|`（🧠第一性原理）。
- ⑤运营策略每条动作格式：`维度 · 优先级 · 动作 · basis(来源) · 竞品回应 · 控制圈 · 时间窗`（🧠博弈论 + 斯多葛）。
- 末尾统一附：本次**数据完备度 x/10** + **判断不了的清单** + **控制圈标注**（可控动作 / 不可控观察哨）+ **下一步推荐**（按 🧭 导航表，2-3 条带为什么）。

---

## ✅ 自检（产出前过一遍）

- [ ] 通篇无频率百分比（"X%的人会…"）、无业绩预测（"涨X%"）。
- [ ] 每条结论有 `[数据]/[推断]/basis`，核心判断带「反证」。
- [ ] 缺数据的维度已降级标注；数据齐的维度给了单一判断、没骑墙。
- [ ] 人群段：真的读了卡片核心段代入，不是名字印象。
- [ ] handoff/导航接上了；已给出下一步，用户授权持久化时本次**存档已写**。
- [ ] 经验库：开工读了先验并催了回填；用户授权持久化时收尾假说回写进 §0（过意义闸）；basis 只取「单验」及以上。

> 认知OS 逐段的必出标注（术语对齐 / 原子树 / 需求线+焦虑面 / 竞品预判 / 控制圈 / 矛盾动态 / 信任透支筛查）以 🧠 速查路由表「必出标注」列为准，不在此重复；复诊纪律（真实 delta 才跑、3 轮护栏）见 🔄 一节。

---

_本文件为**分发 · 自包含版（v1.8.0）**：①②③ 路由指向包内 `子技能/`（主图情境诊断 / 详情页架构师 / 人群验证引擎）；认知OS 正文在 `references/认知OS.md`（按需读取）；v1.8.0 新增症状分诊、任务后导航、`data/诊断存档/` + 回填触发器。实例层角色见同目录 `AGENT.md`（电商运营助手·张发财）。经验库与诊断存档初始为空模板，随你自己的真实诊断积累。_

_📣 电商运营诊断 v1.8.0 · 张发财｜公众号「财哥的电商思维」· 小红书「张发财的资料库」。完整版与更新见同名公众号；转载/分享请保留署名。_
